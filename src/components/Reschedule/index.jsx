import CabSearchModal from '../CabSearchModal';

const Reschedule = () => {
  const value = 0;
  return (
    <div>
      <CabSearchModal
        showButton={false}
      />
    </div>

  );
};

export default Reschedule;
